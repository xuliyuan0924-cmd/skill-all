---
description: 扫描当前项目已有代码，列出可沉淀到功能池的 UI 功能，由设计师选择后录入团队知识库（不是新增功能）
---

请扫描当前项目中**已实现**的 UI 功能模块，不要新增或修改任何代码。

当前项目文件：
!`find . \( -name "*.vue" -o -name "*.jsx" -o -name "*.tsx" -o -name "*.html" \) -not -path "*/node_modules/*" -not -path "*/.git/*" 2>/dev/null | head -50`

根据上方文件，列出项目中**已写好的**可复用 UI 功能（如导航栏、表单、轮播、弹窗等），格式：

- [ ] **① 功能名称** — `文件路径` — 一句话说明
- [ ] **② 功能名称** — `文件路径` — 一句话说明

列完后问我：**选哪些录入功能池？**（回复序号如 `1 3` 或 `全部`）

选好后，逐个读取代码，反推出「让 AI 从零复现这个功能的有效描述」，追问我关键约束和漏说的细节，然后生成 SKILL.md 和 preview.html，最后提交 git PR。

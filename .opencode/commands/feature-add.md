---
description: 扫描当前项目已有代码，列出可沉淀到功能池的 UI 功能，由设计师选择后录入团队知识库（不是新增功能）
---

请完成以下任务，不要新增或修改任何项目代码：

1. 用工具列出当前项目的所有源文件（.vue / .jsx / .tsx / .html），排除 node_modules
2. 读取这些文件，识别其中**已经实现**的独立 UI 功能模块

识别完成后，用以下格式列出清单：

- [ ] **① 功能名称** — `文件路径` — 一句话说明
- [ ] **② 功能名称** — `文件路径` — 一句话说明

然后问我：**选哪些录入功能池？**（回复序号如 `1 3` 或 `全部`）

选好后，逐个读取代码，反推出「让 AI 从零复现这个功能的有效描述」，追问我关键约束和漏说的细节，然后按 `~/feature-pool/TEMPLATE.md` 生成 SKILL.md 和 preview.html，最后提交 git PR。
